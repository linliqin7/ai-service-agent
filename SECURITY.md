# 本地个人项目安全边界

服务只监听 127.0.0.1。禁止将本项目直接部署到公网处理真实客户数据。

DeepSeek 密钥由服务端环境或 macOS 钥匙串读取，不写入源码、前端、trace 或数据库。当前凭据曾出现在聊天中，应在服务商处轮换；启动器继续读取同名钥匙串条目即可。替换密钥可用 macOS 钥匙串访问应用编辑 `brokerage-support-agent.deepseek`，然后重启本项目。

模拟账户登录用于检查工作流，不等于生产鉴权。会话 ID 是本地访问凭证，不分享给他人。退出或切换模拟用户清空私有上下文和当前会话工单。输入仅做基础脱敏，不足以处理任意真实 PII，页面也明确提醒不要输入真实账号、证件、密码或验证码。

模型不持有真实券商接口或交易权限。工具参数通过 Schema 校验，身份由会话绑定，订单归属在模型看到返回值前校验。规则回答采用完整依据句，不能因存在引用而允许自由事实生成。

模型调用失败仅记录稳定错误码，不回显服务商响应正文。实际 token 与请求/响应模型标识可审计；未根据未核验价格编造费用。

会话读取有效期为 24 小时；当前没有自动物理清除机制。可删除本地 var 目录清除所有演示记录（先停止服务），或通过会话删除接口清除指定会话。
