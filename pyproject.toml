[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brokerage-support-agent"
version = "0.2.0"
dependencies = ["pydantic>=2.7", "pydantic-settings>=2.2", "fastapi>=0.115", "uvicorn>=0.30", "httpx>=0.27", "pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
